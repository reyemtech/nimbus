/**
 * State backend abstractions for @reyemtech/nimbus.
 *
 * @module state
 */

export type {
  StateBackendType,
  IReplicationConfig,
  IStateLockConfig,
  IStateBackendConfig,
  IStateBackend,
} from "./interfaces";
