/**
 * Database module — database provisioning abstraction.
 *
 * @module database
 */

export type {
  DatabaseEngine,
  DatabaseMode,
  DatabaseOperator,
  IDatabaseBackupConfig,
  IDatabaseConfig,
  IDatabase,
} from "./interfaces";
